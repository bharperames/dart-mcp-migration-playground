name: dart_react_todo
description: A simple React Todo List in Dart without sound null safety.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  react: any

dev_dependencies:
  build_runner: any
  build_web_compilers: any
